<Window x:Class="Ruffle.MainWindowRuffle"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ruffle"
        mc:Ignorable="d"
        Title="Ruffle" Height="900" Width="1500" Background="DarkSlateBlue">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Canvas x:Name="cvs"/>
        <DockPanel Grid.Row="1">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal" x:Name="spLettersEntered"/>
                <StackPanel Orientation="Horizontal" x:Name="spLettersAvailable"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Margin="100" Content="_Submit" FontSize="30"/>
                    <Button Margin="100" Content="_Clear" FontSize="30"/>
                    <Button Margin="100" Content="_Ruffle" FontSize="30"/>
                    <Button Margin="100" Content="_End" FontSize="30"/>
                </StackPanel>

            </StackPanel>



        </DockPanel>

    </Grid>
</Window>
